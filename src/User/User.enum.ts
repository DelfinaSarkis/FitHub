export enum UserRole {
  ADMIN = 'admin',
  ENTRENADOR = 'entrenador',
  USER = 'user',
}
export enum SolicitudState {
  NONE = 'none',
  PENDING = 'pending',
  CORRECTION = "correction",
  ACCEPTED = 'accepted',
  DENIED= 'denied'
}