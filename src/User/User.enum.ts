export enum UserRole {
  ADMIN = 'admin',
  ENTRENADOR = 'entrenador',
  USER = 'user',
}
export enum SolicitudState {
  None = 'none',
  PENDING = 'pending',
  ACCEPTED = 'accepted',
  DENIED= 'denied'
}